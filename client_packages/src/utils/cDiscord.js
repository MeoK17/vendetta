setInterval(function () {
    //var name = (mp.players.local.getVariable('REMOTE_ID') == undefined) ? `Не авторизован` : `Игрок №${mp.players.local.getVariable("REMOTE_ID")}`;
	mp.discord.update('Vendetta RolePlay', `На стадии разработки`);
}, 10000);